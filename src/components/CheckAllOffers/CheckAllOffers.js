import classes from './CheckAllOffers.module.css'

const CheckAllOffers = () => {

    return (
        <a className={classes.offerButton} href="#"> Check All Offers </a>
    )
}

export default CheckAllOffers